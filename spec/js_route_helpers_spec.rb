require 'spec_helper'

describe JsRouteHelpers do
  it 'has a version number' do
    expect(JsRouteHelpers::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
